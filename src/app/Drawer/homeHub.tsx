import { ScrollView, View } from 'react-native';

export default function HomeHub() {
    return (
        <ScrollView className=' flex-1'>
            <View className=''>

            </View>
        </ScrollView>);
}